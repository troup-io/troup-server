endpoint: http://localhost:1337
datamodel: ../datamodel/datamodel.graphql
secret: ${env:PRISMA_SECRET}
generate:
  - generator: typescript-client
    output: ../src/generated/prisma-client/

hooks:
    post-deploy:
        - graphql get-schema
